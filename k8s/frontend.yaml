apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: annotations
    tier: frontend
spec:
  ports:
  - port: 443
    targetPort: 8080
    protocol: TCP
  selector:
    app: annotations
    tier: frontend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  selector:
    matchLabels:
      app: annotations
      tier: frontend
  replicas: 6
  template:
    metadata:
      labels:
        app: annotations
        tier: frontend
    spec:
      containers:
      - name: frontend
        image: us.icr.io/bourdakos1/annotate:b5437d151225b149c9cd1c3bed34f9dd8723f21d
        env:
        - name: TLS_CRT
          valueFrom:
            secretKeyRef:
              name: stagingannotations
              key: tls.crt
        - name: TLS_KEY
          valueFrom:
            secretKeyRef:
              name: stagingannotations
              key: tls.key
        ports:
        - containerPort: 8080
