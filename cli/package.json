{
  "name": "@iris/cli",
  "version": "0.0.2",
  "bin": {
    "iris": "./bin/index.js"
  },
  "scripts": {
    "postinstall": "pwd && git clone https://github.com/cloud-annotations/iris.git /usr/local/.iris && cd /usr/local/.iris && git checkout helm && yarn install && make build"
  },
  "publishConfig": {
    "access": "public"
  }
}
