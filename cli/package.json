{
  "name": "@iris/cli",
  "version": "0.0.3",
  "bin": {
    "iris": "./bin/index.js"
  },
  "scripts": {
    "postinstall": "git clone https://github.com/cloud-annotations/iris.git && cd iris && git checkout helm && yarn install && make build"
  },
  "publishConfig": {
    "access": "public"
  }
}
