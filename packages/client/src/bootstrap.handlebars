import initReactApp from "./"
import initIris from "./initIris"

initIris()

{{#each extensions}}
try {
  const {{clean this}} = require("{{this}}")
  {{clean this}}.default.activate(window.IRIS)
} catch (e) {
  console.log(e)
}
{{/each}}

initReactApp()