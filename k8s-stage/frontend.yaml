apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: annotations
    tier: frontend
spec:
  ports:
  - port: 443
    targetPort: 8080
    protocol: TCP
  selector:
    app: annotations
    tier: frontend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  selector:
    matchLabels:
      app: annotations
      tier: frontend
  replicas: 6
  template:
    metadata:
      labels:
        app: annotations
        tier: frontend
    spec:
      containers:
      - name: frontend
        image: us.icr.io/bourdakos1/annotate:a766998a45e469393737b36b48927d5860ccc1d6
        env:
        - name: TLS_CRT
          valueFrom:
            secretKeyRef:
              name: stagingannotations
              key: tls.crt
        - name: TLS_KEY
          valueFrom:
            secretKeyRef:
              name: stagingannotations
              key: tls.key
        ports:
        - containerPort: 8080
